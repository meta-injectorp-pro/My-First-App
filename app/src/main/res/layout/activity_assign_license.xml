<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F0F4F8"
    tools:context=".AssignLicenseActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Assign New License"
            android:textSize="24sp"
            android:textStyle="bold"
            android:textColor="@color/black"
            android:layout_marginBottom="16dp"/>

        <TextView style="@style/FormLabel" android:text="Next Available Key (Read-only)"/>
        <EditText android:id="@+id/etAssignLicenseKey" style="@style/FormEditText.ReadOnly"/>

        <TextView style="@style/FormLabel" android:text="Customer Name"/>
        <EditText android:id="@+id/etAssignCustomerName" style="@style/FormEditText" android:inputType="textPersonName"/>

        <TextView style="@style/FormLabel" android:text="Email"/>
        <EditText android:id="@+id/etAssignEmail" style="@style/FormEditText" android:inputType="textEmailAddress"/>

        <TextView style="@style/FormLabel" android:text="Phone Number"/>
        <EditText android:id="@+id/etAssignPhoneNumber" style="@style/FormEditText" android:inputType="phone"/>

        <TextView style="@style/FormLabel" android:text="Package"/>
        <Spinner
            android:id="@+id/spinnerAssignPackage"
            style="@style/FormSpinner"
            android:entries="@array/package_options"/>

        <TextView style="@style/FormLabel" android:text="Duration"/>
        <Spinner
            android:id="@+id/spinnerAssignDuration"
            style="@style/FormSpinner"
            android:entries="@array/duration_options"/>

        <TextView style="@style/FormLabel" android:text="Sale Price (BDT)"/>
        <EditText
            android:id="@+id/etAssignSalePrice"
            style="@style/FormEditText"
            android:hint="Enter amount in BDT"
            android:inputType="numberDecimal"/>

        <Button
            android:id="@+id/btnConfirmAssignment"
            android:layout_width="match_parent"
            android:layout_height="55dp"
            android:layout_marginTop="24dp"
            android:text="Confirm Assignment"/>

    </LinearLayout>
</ScrollView>